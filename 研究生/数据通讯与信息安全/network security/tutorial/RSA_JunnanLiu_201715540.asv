httpsurl="http://primes.utm.edu/lists/small/1000.txt";
[N,d,e]=RSA_Gen(5,7);
function [N,d,e] = RSA_Gen(p,q)
    N=p*q;
    z=(p-1)*(q-1);
    for e=N-2:-1:2
        if(gcd(e,z))
            break;
        end
    end
    disp(e);
    for d=1:1:z
        if(mod(e*d,z)==1)
            break;
        end
    end
    disp(d);
end

